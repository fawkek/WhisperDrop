# Local model storage

WhisperDrop keeps the bundled tokenizer source files in this directory.
Core ML model weights are downloaded at runtime to Application Support and are intentionally excluded from Git because they are about 1.62 GB.
